<!DOCTYPE html>
<html>
<head>
  <title>TimeClock</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body class="<%= controller_name %> <%= action_name %>">
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "TimeClock", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/users/new">Register <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <%= link_to 'Timeclock', new_time_count_path, class: 'nav-link'%>
      </li>
      <li class="nav-item">
        <%= link_to 'Audit', users_path, class: 'nav-link'%>
      </li>
    </ul>
  </div>
</nav>
<div class="container-fluid" id="flash_msg">
  <% flash.each do |key, value| %>
    <% if key == "alert" %>
      <%= content_tag(:div, value, class: "alert alert-danger") %>
    <% elsif key == "notice" %>
      <%= content_tag(:div, value, class: "alert alert-success") %>
    <% end %>
  <% end %>
</div>
<div class="container" id="main-div">
  <%= yield %>
</div>
</body>
</html>
